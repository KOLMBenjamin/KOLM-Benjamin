function f() {
  const u = 0;
  while(u <20){
      let zufallszahl = Math.round(Math.random() *20 +1);
      if(zufallszahl > 100){
          throw new Error ("Fehler!");
          return fertigstellung;
      }
      u++;
  }
}
try {error}

catch (error) {console.log(error)}